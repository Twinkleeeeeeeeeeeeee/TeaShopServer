<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hmjy.mapper.BannerPicMapper">
    <select id="getBannerPic" resultType="com.hmjy.pojo.BannerPic">
        select a.id,a.paixu,a.picUrl,a.remark,a.status,a.statusStr,a.title,a.type from banner_list a
    </select>
</mapper>