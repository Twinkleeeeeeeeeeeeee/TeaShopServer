<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hmjy.mapper.GetOrderListMapper">
    <select id="getOrderList" resultType="UserOrder">
        select a.userId,a.orderId,a.provinceId,a.cityId,a.districtId,a.address,a.linkMan,a.mobile,a.code,a.allGoodsPrice,a.dateAdd from user_order_info a
    </select>
</mapper>